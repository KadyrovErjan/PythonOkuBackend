upstream django_app {
    server web:8000;
}

server {
    listen 80;
    server_name _;
    
    # Увеличиваем размер загружаемых файлов
    client_max_body_size 100M;
    
    # Логирование
    access_log /var/log/nginx/itadis_access.log;
    error_log /var/log/nginx/itadis_error.log;
    
    # Основное приложение
    location / {
        proxy_pass http://django_app;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_redirect off;
        
        # Таймауты
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
    
    # Статические файлы
    location /static/ {
        alias /app/staticfiles/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
    
    # Медиа файлы
    location /media/ {
        alias /app/media/;
        expires 7d;
        add_header Cache-Control "public";
    }
    
    # Запрет доступа к скрытым файлам
    location ~ /\. {
        deny all;
        access_log off;
        log_not_found off;
    }
}

# HTTPS конфигурация (раскомментировать для production с SSL)
# server {
#     listen 443 ssl http2;
#     server_name itadis.kg www.itadis.kg;
#     
#     ssl_certificate /etc/nginx/ssl/cert.pem;
#     ssl_certificate_key /etc/nginx/ssl/key.pem;
#     
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#     
#     # Остальная конфигурация аналогична server блоку выше
#     client_max_body_size 100M;
#     
#     location / {
#         proxy_pass http://django_app;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto https;
#         proxy_redirect off;
#     }
#     
#     location /static/ {
#         alias /app/staticfiles/;
#         expires 30d;
#     }
#     
#     location /media/ {
#         alias /app/media/;
#         expires 7d;
#     }
# }
